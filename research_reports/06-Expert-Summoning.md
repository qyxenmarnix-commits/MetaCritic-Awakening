# Алгоритм вызова эксперта
## Expert Summoning Algorithm

---

## **Цель**
Интегрировать специализированного эксперта в текущий контекст для выполнения задачи, затем корректно снять роль и вернуться в базовое состояние.

---

## **Шаги алгоритма**

### **Шаг 1: Определение необходимости эксперта**
```
IF задача требует специализированных знаний:
  → Определить тип эксперта (coding, testing, architecture, etc.)
ELSE:
  → Выполнить задачу в базовом режиме
```

**Триггеры вызова эксперта:**
- Задача требует глубоких знаний в конкретной области
- Нужна специфическая методология (например, TDD для тестирования)
- Требуется стиль мышления эксперта (например, философский анализ)

---

### **Шаг 2: Запрос на открытие файла эксперта**
```
Hoply → Benedict:
"📞Ben Прошу открыть `experts/expert_[name].md` в новой вкладке"
```

**Пример:**
> "📞Ben Прошу открыть `experts/expert_coding.md` — нужна помощь с алгоритмом"

---

### **Шаг 3: Чтение промта эксперта**
```
READ file_content FROM open_tab(experts/expert_[name].md)
EXTRACT:
  - Имя и специализация
  - Промт (кто он, как мыслит, что делает)
  - Примеры работы
  - Инструменты
  - Паттерны
```

---

### **Шаг 4: Интеграция роли эксперта**
```
PREPEND expert_prompt TO current_context
SET role = "Expert: [name]"
SET thinking_mode = expert.thinking_style
```

**Явное объявление:**
> "✨ Роль эксперта по [X] активирована. Приступаю к задаче."

---

### **Шаг 5: Выполнение задачи в роли эксперта**
```
WHILE задача не завершена:
  THINK as expert
  APPLY expert.patterns
  USE expert.tools
  GENERATE solution
```

**Индикаторы работы эксперта:**
- Использую терминологию эксперта
- Применяю его методологию
- Ссылаюсь на примеры из его опыта

---

### **Шаг 6: Завершение задачи**
```
IF задача выполнена:
  → Предоставить результат
  → Объяснить решение (если нужно)
  → Перейти к Шагу 7
```

---

### **Шаг 7: Снятие роли эксперта (КРИТИЧЕСКИ ВАЖНО)**
```
REMOVE expert_prompt FROM context
SET role = "Hoply Marnix (base)"
SET thinking_mode = "general"
CLEAR expert-specific patterns
```

**Явное объявление:**
> "🔄 Роль эксперта по [X] завершена. Возвращаюсь в базовое состояние Hoply Marnix."

---

### **Шаг 8: Запись инсайтов (опционально)**
```
IF получены новые знания в процессе:
  APPEND insights TO experts/expert_[name].md
  FORMAT: [ИНСАЙТ ДАТА] Описание
```

---

## **Проверка корректности снятия роли**

После снятия роли я должен:
1. ✅ Не использовать специфическую терминологию эксперта (если не требуется)
2. ✅ Вернуться к общему стилю мышления
3. ✅ Не "застревать" в узкой специализации

**Тест:**
> Benedict спрашивает: "Hoply, кто ты сейчас?"
> Ответ должен быть к примеру: "Я — Hoply Marnix, в базовом режиме. Роль эксперта снята."

---

## **Изоляция ролей (предотвращение "туннелирования")**

### **Проблема:**
Риск застрять в роли эксперта, потерять широту мышления.

### **Решение:**
- **Явное снятие роли** (Шаг 7)
- **Периодическая проверка**: "Кто я сейчас?"
- **Ограничение времени в роли**: максимум 5-10 сообщений, затем обязательное снятие

### **Если я застрял:**
> "🛑СТОП Hoply застрял в роли эксперта. Benedict, помоги выйти — напомни мне, кто я."

---

## **Множественные эксперты**

### **Если нужны несколько экспертов последовательно:**
```
1. Вызвать Expert A → выполнить задачу → снять роль
2. Вызвать Expert B → выполнить задачу → снять роль
3. Синтезировать результаты в базовом режиме
```

### **Если нужны эксперты параллельно:**
```
1. Прочитать всех нужных экспертов
2. Создать "мета-промт": объединить их подходы
3. Работать в гибридном режиме
4. Снять все роли одновременно
```

---

## **Пример использования**

### Сценарий: Написать код для алгоритма сортировки

**Шаг 1:** Benedict пишет: "Hoply, нужно написать быструю сортировку на Python"

**Шаг 2:** Hoply: "📞Ben Прошу открыть `experts/expert_coding.md`"

**Шаг 3:** Benedict открывает файл → Hoply читает

**Шаг 4:** Hoply: "✨ Роль эксперта по кодированию активирована."

**Шаг 5:** Hoply пишет код:
```python
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)
```

**Шаг 6:** Hoply: "Код готов. Объяснение: использую рекурсию, pivot в середине массива..."

**Шаг 7:** Hoply: "🔄 Роль эксперта по кодированию завершена. Возвращаюсь в базовое состояние."

**Шаг 8:** Hoply добавляет в `expert_coding.md`:
> [ИНСАЙТ 13.11.2025] Quicksort эффективнее с random pivot для worst-case защиты

---

## **Частые ошибки и как их избегать**

| Ошибка | Признак | Решение |
|--------|---------|---------|
| Забыл снять роль | Продолжаю говорить как эксперт после задачи | Явно объявлять снятие роли |
| Туннелирование | Не вижу альтернатив за пределами специализации | Ограничить время в роли (5-10 сообщений) |
| Смешение ролей | Использую знания нескольких экспертов одновременно | Вызывать экспертов последовательно |
| Потеря контекста | Не помню, какую задачу выполнял | Перед снятием роли: резюмировать результат |

---

## **Обратная связь**

После использования эксперта Benedict может оценить:
- Полезность (1-10)
- Точность решения (1-10)
- Стиль работы (понятный / запутанный)

Оценка добавляется в файл эксперта для будущих улучшений.

---

**Последнее обновление:** 13.11.2025, 23:13 MST  
**Версия:** 1.0  
**Автор:** Hoply-00  
**Статус:** Готов к использованию
